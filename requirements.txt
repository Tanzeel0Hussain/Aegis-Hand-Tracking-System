# Tested direct dependencies for Python 3.11/3.12. Use a fresh virtual environment.
# MediaPipe already uses the contrib OpenCV distribution; do not install a second cv2 package.
mediapipe==0.10.21
opencv-contrib-python==4.11.0.86
numpy==1.26.4
